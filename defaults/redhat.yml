---
cwa_download_url: "https://s3.amazonaws.com/amazoncloudwatch-agent/redhat/{{ 'arm64' if ansible_architecture == 'aarch64' else 'amd64' }}/latest"
cwa_pkg_file: amazon-cloudwatch-agent.rpm
cwa_sig_file: amazon-cloudwatch-agent.rpm.sig
